/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "static7.tgstat.ru"], // List of allowed domains for images
  },
};

export default nextConfig;
